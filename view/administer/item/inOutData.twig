<html>
  <head>
    <meta charset="utf-8"/>
    <link rel="stylesheet" type="text/css" href="../../../common/css/style.css">
    <title></title>
  </head>
  <body>
    {% include "administer/header.twig" %}
    <h3>入出庫一覧ページ</h3>

    <form method="POST">
      <label>開始日: <input type="date" name="start_date" value="{{ start_date }}"></label>
      <label>終了日: <input type="date" name="end_date" value="{{ end_date }}"></label>
      <button type="submit">検索</button>
    </form>


    {% if productArr %}
      <table border="1" cellpadding="5" cellspacing="0">
        <thead>
          <tr>
            <th>日時</th>
            <th>顧客番号</th>
            <th>種別</th>
            <th>理由</th>
            <th>商品名</th>
            <th>数量</th>
          </tr>
        </thead>
        <tbody>
          {% for value in productArr %}
            <tr>
              <td>{{ value.created_at }}</td>
              <td>{{ value.customer_no }}</td>
              <td>{{ value.change_type }}</td>
              <td>{{ value.reason }}</td>
              <td>{{ value.product_name }}</td>
              <td>{{ value.quantity }}</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    {% else %}
      <p>該当するデータがありません。</p>
    {% endif %}

    <br><a href="../../../controller/administer/index.php">トップページへ戻る</a>
    {% include "administer/footer.twig" %}
  </body>
</html>