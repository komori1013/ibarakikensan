<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="../../../common/css/style.css">
    <title></title>
  </head>
  <body>
    {% include "header.twig" %}
    <h3>売上集計ページ</h3>

    <form method="POST">
      <label>開始日: <input type="date" name="start_date" value="{{ start_date }}"></label>
      <label>終了日: <input type="date" name="end_date" value="{{ end_date }}"></label>
      <button type="submit">検索</button>
    </form>

<form method="POST" action="../../../controller/administer/item/exportCsv.php">
  <input type="hidden" name="productArr" value='{{ productArr|json_encode|e }}'>
  CSV:<button type="submit">出力</button>
</form>
</br>

{% if productArr %}
  <table border="1" cellpadding="5" cellspacing="0">
    <thead>
      <tr>
        <th>日時</th>
        <th>顧客番号</th>
        <th>種別</th>
        <th>理由</th>
        <th>商品名</th>
        <th>数量</th>
        <th>単価</th>
        <th>合計</th>
      </tr>
    </thead>
    <tbody>
      {% for row in productArr %}
        <tr>
          <td>{{ row.created_at }}</td>
          <td>{{ row.customer_no }}</td>
          <td>{{ row.change_type }}</td>
          <td>{{ row.reason }}</td>
          <td>{{ row.product_name }}</td>
          <td>{{ row.quantity }}</td>
          <td>{{ row.price| number_format(0, '.', ',') }}</td>
          <td>{{ row.subtotal|number_format(0, ".", ",") }}</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>

  <p><strong>売上合計個数：</strong>{{ totalQuantity }}個</p>
  <p><strong>売上合計金額：</strong>{{ totalAmount | number_format(0, '.', ',') }} 円</p>

{% else %}
  <p>該当するデータがありません。</p>
{% endif %}
   <br><a href="../../../controller/administer/index.php">トップページへ戻る</a>
    {% include "footer.twig" %}
      </body>
</html>