<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="../../../common/css/style.css">
    <title>登録者一覧</title>
  </head>
  <body>
    {% include "header.twig" %}
    <h3>登録者一覧ページ</h3>

    {% if dataArr is not empty %}
      <table border="1" cellpadding="5" cellspacing="0">
        <thead>
          <tr>
            <th>ID</th>
            <th>アカウント</th>
            <th>パスワード</th>
            <th>名前</th>
            <th>メールアドレス</th>
            <th>登録日時</th>
            <th>編集リンク</th>
          </tr>
        </thead>
        <tbody>
          {% for value in dataArr %}
            <tr>
              <td>{{ value.member_id }}</td>
              <td>{{ value.member_account }}</td>
              <td>{{ value.member_password }}</td>
              <td>
                <span style="font-size: 60%;">{{ value.family_name_kana }}{{ value.first_name_kana }}</span><br>
                {{ value.family_name }} {{ value.first_name }}
              </td>
              <td>{{ value.email }}</td>
              <td>{{ value.created_at }}</td>
              <td><a href="#">編集</a></td>
            </tr>
          {% endfor %}
        </tbody>
      </table>

      <p><strong>登録者数合計：</strong>{{ dataArr | length }} 名</p>
    {% else %}
      <p>登録者が存在しません。</p>
    {% endif %}

    <br><a href="../../../controller/administer/index.php">トップページへ戻る</a>
    {% include "footer.twig" %}
  </body>
</html>